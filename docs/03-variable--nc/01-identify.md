## identify

### word

first: `'a'..'z' | 'A'..'Z' | "_"`
then: `'0'..'9' | "-" | "*" | "!" | "?" | "."`

### special word

`% + - & | ~ // / << <= <> < >> >= > = ** * ?? _ ..`

### delimiters

```
WHITESPACE | "[" | "]" | "(" | ")" | ";" | "{" | "\"" | "#(" | "![" | "#{" | EOI
```
